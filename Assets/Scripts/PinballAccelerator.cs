using UnityEngine;

public class PinballAccelerator : MonoBehaviour
{
    [SerializeField]
    public float forceAmount = 500.0f;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
